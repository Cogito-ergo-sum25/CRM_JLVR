{{template "base" .}}

{{define "content"}}
{{$totalC := index .Data "totalContactos"}}
{{$totalD := index .Data "totalDinero"}}
{{$cumples := index .Data "cumpleaneros"}}

<div class="max-w-6xl mx-auto">
    <h2 class="text-2xl font-bold text-gray-800 mb-6">Resumen del Despacho</h2>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
        <div class="bg-white p-6 rounded-2xl shadow-sm border-l-8 border-indigo-500">
            <p class="text-sm text-gray-500 font-bold uppercase">Expedientes Totales</p>
            <p class="text-3xl font-black text-gray-800">{{$totalC}}</p>
        </div>

        <div class="bg-indigo-600 p-6 rounded-2xl shadow-lg text-white flex items-center justify-between">
            <div>
                <p class="font-bold">¿Nuevo Cliente?</p>
                <a href="/nuevo-contacto" class="text-xs underline opacity-80 hover:opacity-100">Registrar ahora →</a>
            </div>
            <span class="text-4xl">📝</span>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <div class="bg-white rounded-2xl shadow-sm p-6 border border-gray-100">
            <div class="flex items-center justify-between mb-4">
                <h3 class="font-bold text-gray-700 flex items-center">
                    <span class="mr-2">🎂</span> Cumpleaños del Mes
                </h3>
                <span class="text-[10px] bg-pink-100 text-pink-600 px-2 py-1 rounded-full font-bold uppercase">Atención</span>
            </div>
            
            <div class="space-y-4">
                {{range $cumples}}
                <div class="flex items-center justify-between p-3 hover:bg-gray-50 rounded-xl transition">
                    <div class="flex items-center">
                        <div class="w-10 h-10 bg-pink-50 text-pink-500 rounded-full flex items-center justify-center font-bold mr-3 text-sm">
                            {{if .Nombre}}
                                {{slice .Nombre 0 1}}
                            {{else}}
                                ?
                            {{end}}
                        </div>
                        <div>
                            <p class="text-sm font-bold text-gray-800">{{.Nombre}}</p>
                            <p class="text-xs text-gray-500">
                                {{if .FechaCumpleanios}}
                                    Celebra el {{.FechaCumpleanios.Format "02 de January"}}
                                {{end}}
                            </p>
                        </div>
                    </div>
                </div>
                {{else}}
                <p class="text-sm text-gray-400 italic text-center py-8">No hay cumpleaños este mes.</p>
                {{end}}
            </div>
        </div>

        <div class="bg-white rounded-2xl shadow-sm p-6 border border-gray-100">
            <h3 class="font-bold text-gray-700 mb-4 flex items-center">
                <span class="mr-2">🚀</span> Acciones Frecuentes
            </h3>
            <div class="grid grid-cols-2 gap-4">
                <a href="/contactos" class="p-4 bg-gray-50 rounded-xl hover:bg-indigo-50 transition border border-gray-100 text-center">
                    <p class="text-2xl mb-1">📇</p>
                    <p class="text-xs font-bold text-gray-600">Ver Directorio</p>
                </a>
                <a href="/calendario" class="p-4 bg-gray-50 rounded-xl hover:bg-pink-50 transition border border-gray-100 text-center">
                    <p class="text-2xl mb-1">📅</p>
                    <p class="text-xs font-bold text-gray-600">Ver Agenda</p>
                </a>
                <a href="/reportes" class="p-4 bg-gray-50 rounded-xl hover:bg-emerald-50 transition border border-gray-100 text-center opacity-50 cursor-not-allowed">
                    <p class="text-2xl mb-1">📊</p>
                    <p class="text-xs font-bold text-gray-600">Reportes (Próximamente)</p>
                </a>
                <a href="/ajustes" class="p-4 bg-gray-50 rounded-xl hover:bg-gray-200 transition border border-gray-100 text-center opacity-50 cursor-not-allowed">
                    <p class="text-2xl mb-1">⚙️</p>
                    <p class="text-xs font-bold text-gray-600">Ajustes</p>
                </a>
            </div>
        </div>
    </div>
</div>
{{end}}